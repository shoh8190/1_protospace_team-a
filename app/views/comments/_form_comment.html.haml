.row.proto-description
  %h4.comments-total
    Comments
    %span.num
      = @prototype.comments.length
  .comment-zone#comment-area
    .comment-list
      - @prototype.comments.each do |comments|
        .media
          .media-left
            = comments.user.avatar
          .media-body
            %h4.media-heading#top-aligned-media
              = comments.user.name
            %p
              = comments.body
          -if user_signed_in?
            -if comments.user_id == current_user.id
              %ul.media-action
                %li= link_to 'EDIT'
                %li= link_to 'DELETE'
    = form_for [@prototype, @comment], html: {class: "comment-submit"} do |f|
      = f.text_field :body, class: "comment-input"
      = f.submit 'COMMENT'
